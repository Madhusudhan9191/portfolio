"use client";
import Image from "next/image";
import { AnimatePresence, motion } from "framer-motion";
import { X } from "lucide-react";
import { useState } from "react";
const steps=["Invoice queue","Document preview","OCR extraction","GST validation","Gemma AI investigation","Evidence review","Compliance decision","Report generation"];
export default function ChakraWorkflow(){const [open,setOpen]=useState(false);return <section id="workflow" className="pb-20"><div className="mx-auto max-w-[1000px] px-6"><div className="eyebrow">Investigation workflow</div><h2 className="mt-2 text-3xl">From received invoice to accountable decision.</h2><div className="mt-7 grid gap-6 md:grid-cols-[1.25fr_.75fr]"><button onClick={()=>setOpen(true)} aria-label="Open Invoice Investigation Workspace image" className="group surface overflow-hidden rounded-2xl text-left"><Image src="/images/chakra/investigation-workspace.png" alt="CHAKRA Invoice Investigation Workspace with invoice review, OCR validation, AI risk indicators, and incident timeline" width={1024} height={1024} sizes="(max-width: 768px) 100vw, 65vw" className="h-auto w-full transition-transform duration-500 group-hover:scale-[1.02]"/><span className="block border-t px-4 py-3 text-xs" style={{borderColor:"var(--border)",color:"var(--text-dimmer)"}}>AI-powered investigation console with invoice review, OCR validation, Gemma reasoning, and incident timeline.</span></button><div className="grid gap-2">{steps.map((step,index)=><motion.div key={step} initial={{opacity:0,x:10}} whileInView={{opacity:1,x:0}} viewport={{once:true}} transition={{delay:index*.04}} className="rounded-xl border p-3 text-sm" style={{borderColor:"var(--border)",background:"var(--glass)"}}><span className="mono mr-3 text-xs" style={{color:"var(--accent)"}}>{String(index+1).padStart(2,"0")}</span>{step}</motion.div>)}</div></div><AnimatePresence>{open&&<motion.div role="dialog" aria-modal="true" aria-label="Invoice Investigation Workspace" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setOpen(false)} className="fixed inset-0 z-[10000] flex items-center justify-center bg-black/85 p-4"><motion.div onClick={e=>e.stopPropagation()} initial={{scale:.96}} animate={{scale:1}} exit={{scale:.96}} className="relative max-h-full max-w-5xl"><button onClick={()=>setOpen(false)} aria-label="Close workspace image" className="absolute -top-10 right-0 p-2 text-white"><X/></button><Image src="/images/chakra/investigation-workspace.png" alt="CHAKRA Invoice Investigation Workspace" width={1024} height={1024} priority className="max-h-[85vh] w-auto rounded-xl object-contain"/></motion.div></motion.div>}</AnimatePresence></div></section>}
